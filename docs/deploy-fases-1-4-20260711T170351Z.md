# Deploy Fases 1-4 em produção — Fase 5

Timestamp UTC de início: 2026-07-11T17:03:51Z
Projeto Supabase: `fdsojpwznvephwdoghbn`

## Migrations aplicadas

1. `supabase-image-metadata-schema.sql` — adiciona `image_source_url`,
   `image_credit`, `image_origin`, `image_hash`, `image_width`,
   `image_height` (todas nullable) a `public.posts`, mais
   `posts_image_hash_idx` e a constraint `posts_image_origin_check`.
   Canal: Supabase MCP (`apply_migration`), confirmado ligado ao projeto
   correto por match exato de contagens (13 categorias / 16 tags / 13
   posts / post conhecido presente).
2. `supabase-storage-schema.sql` — cria o bucket público `editorial-images`
   e a policy de leitura pública `editorial-images public read`.

SHA-256 dos arquivos SQL:
- `supabase-image-metadata-schema.sql`: `3234530a8523509f7fa8ca3c917c42413a4999580cb6513370fe44bd852c2e37`
- `supabase-storage-schema.sql`: `0a43934abc82053ab56944654de77d70b7f0b42b071864664cd21b5885569dde`

## PIDs e restart counts

| Processo | PID antes | PID depois | restart_time antes | restart_time depois |
|---|---|---|---|---|
| v360-dashboard | 1096 | 1096 (inalterado) | 0 | 0 |
| vtres60 | 1107 | 1107 (inalterado) | 0 | 0 |
| vtres60-blog | 139476 | 1749002 | 2 | 3 |
| vtres60-agent-worker | 139470 | 1751593 | 1 | 1 |

## Ambiente

- Node: v24.18.0
- npm: 11.16.0
- sharp: 0.34.5 (funcional, confirmado antes e depois de `npm ci`)

## Resultado dos smoke tests

- Storage smoke test (upload/GET/remoção de objeto temporário): **PASS**
- Blog: home 200, `/blog/noticias` 200, hero = post mais recente (correto),
  RIMA 200 sem frase de parceria falsa, E-commerce 200 sem bloco de
  impacto fantasma (bug do build antigo, corrigido pelo novo código),
  categorias 200, empresas 200, nenhum "Crédito não informado" exibido.
- Agent dry-run em produção (`scripts/fase5-dry-run.mts`): **PASS** —
  pipeline completa exercitada com Supabase/LLM/Storage reais, WEG
  corretamente excluída de `companies` mesmo com domínio mencionado,
  imagem processada via Pexels (2150×1000, hash real, upload+remoção real),
  **zero posts criados** (13 antes, 13 depois), Publisher nunca invocado.

## Bucket e policies

- Bucket: `editorial-images`, público, único bucket do projeto.
- Policy: `editorial-images public read` (SELECT, `bucket_id = 'editorial-images'`),
  única policy existente em `storage.objects` — sem INSERT/UPDATE/DELETE
  para `anon`.

## Backup

- Diretório: `/home/pedro/vtres60-blog-deploy-backup-20260711T170351Z/`
- Contém: `.next` anterior (`next-build/`), `package.json`,
  `package-lock.json`, `ecosystem.config.js`, `agent-worker.mjs`, os 4 SQLs,
  `src/`, `scripts/`, `docs/`, e `.env.local`/`.env.production` (em
  `env-secrets/`, `chmod 700`/`600`).
- Manifest: `deploy-manifest.json` no mesmo diretório.

## Rollback

Documentado em `docs/rollback-fase5-20260711T170351Z.md`. Não executado —
não foi necessário.

## Estado final do worker

Online, PID 1751593, cron confirmado para 05:00 e 17:00 America/Sao_Paulo,
mirando `http://localhost:3002/blog/api/agent/cron`. Nenhuma execução
disparada pelo restart em si (node-cron só agenda, não dispara na
inicialização). Próxima execução programada: 20:00 UTC (17:00
America/Sao_Paulo) do mesmo dia.
