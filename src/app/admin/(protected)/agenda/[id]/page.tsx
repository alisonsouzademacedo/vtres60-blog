import { notFound } from "next/navigation";import { AdminPageHeading } from "@/components/admin/page-heading";import { OperationsEditor } from "@/components/admin/operations-editor";import { operationsRepository } from "@/services/operations";
export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params;const[item,events]=await Promise.all([operationsRepository.getEvent(id),operationsRepository.listEvents()]);if(!item)notFound();return <><AdminPageHeading eyebrow="Agenda" title="Editar evento" description={`Atualize a página editorial de “${item.name}”.`}/><OperationsEditor kind="event" initial={item} events={events}/></>}
