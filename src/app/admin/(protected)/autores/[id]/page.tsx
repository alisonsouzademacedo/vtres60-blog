import { notFound } from "next/navigation";import { AdminPageHeading } from "@/components/admin/page-heading";import { TaxonomyEditor } from "@/components/admin/taxonomy-editor";import { editorialRepository } from "@/services/editorial";
export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params;const item=await editorialRepository.getAuthor(id);if(!item)notFound();return <><AdminPageHeading eyebrow="Equipe editorial" title={`Editar ${item.name}`} description="Atualize perfil, credenciais e informações públicas do autor."/><TaxonomyEditor kind="author" initial={item}/></>}
