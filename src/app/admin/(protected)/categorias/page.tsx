import { AdminPageHeading } from "@/components/admin/page-heading";import { CrudList } from "@/components/admin/crud-list";import { editorialRepository } from "@/services/editorial";
export default async function Page(){const items=await editorialRepository.listCategories();return <><AdminPageHeading eyebrow="Taxonomia" title="Categorias" description="Organize navegação, hubs editoriais e páginas de autoridade temática."/><CrudList initial={items.map(item=>({id:item.id,title:item.name,slug:item.slug,meta:`Ordem ${item.menuOrder} · ${item.showInMenu?"No menu":"Fora do menu"}`}))} basePath="/admin/categorias" apiPath="/api/admin/categories" createLabel="Nova categoria" entityName="Categoria" feminine/></>}
