import { notFound } from "next/navigation";import { AdminPageHeading } from "@/components/admin/page-heading";import { ContentEditor } from "@/components/admin/content-editor";import { editorialRepository } from "@/services/editorial";
export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params;const[item,categories,authors,tags,articles]=await Promise.all([editorialRepository.getEducationalArticle(id),editorialRepository.listCategories(),editorialRepository.listAuthors(),editorialRepository.listTags(),editorialRepository.listEducationalArticles()]);if(!item)notFound();return <><AdminPageHeading eyebrow="Conteúdo evergreen" title="Editar artigo" description={`Atualize “${item.title}”, seus relacionamentos e SEO.`}/><ContentEditor kind="article" initial={item} categories={categories} authors={authors} tags={tags} articleOptions={articles}/></>}
