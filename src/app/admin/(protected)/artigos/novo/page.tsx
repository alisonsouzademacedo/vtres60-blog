import { AdminPageHeading } from "@/components/admin/page-heading";import { ContentEditor } from "@/components/admin/content-editor";import { editorialRepository } from "@/services/editorial";
export default async function Page(){const[categories,authors,tags,articles]=await Promise.all([editorialRepository.listCategories(),editorialRepository.listAuthors(),editorialRepository.listTags(),editorialRepository.listEducationalArticles()]);return <><AdminPageHeading eyebrow="Conteúdo evergreen" title="Novo artigo educativo" description="Crie um conteúdo aprofundado, interligado e preparado para autoridade orgânica."/><ContentEditor kind="article" categories={categories} authors={authors} tags={tags} articleOptions={articles}/></>}
