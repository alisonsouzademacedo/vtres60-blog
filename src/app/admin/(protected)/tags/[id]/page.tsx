import { notFound } from "next/navigation";import { AdminPageHeading } from "@/components/admin/page-heading";import { TaxonomyEditor } from "@/components/admin/taxonomy-editor";import { editorialRepository } from "@/services/editorial";
export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params;const item=await editorialRepository.getTag(id);if(!item)notFound();return <><AdminPageHeading eyebrow="Taxonomia" title={`Editar ${item.name}`} description="Atualize descrição e metadados da tag."/><TaxonomyEditor kind="tag" initial={item}/></>}
