import type { Metadata } from "next";import { notFound } from "next/navigation";import { TaxonomyPage, hasTaxonomyMatches } from "@/components/editorial/taxonomy-page";import { editorialRepository } from "@/services/editorial";import { slugify } from "@/lib/content";
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params;const item=await editorialRepository.getTag(slug);if(!item)return{};const hasMatches=await hasTaxonomyMatches(article=>article.tags.some(tag=>slugify(tag)===slug));return{title:item.metaTitle,description:item.metaDescription,alternates:{canonical:`/tags/${slug}`},...(hasMatches?{}:{robots:{index:false,follow:true}})}}
export default async function Page({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const item=await editorialRepository.getTag(slug);if(!item)notFound();return <TaxonomyPage eyebrow="Assunto" title={item.name} description={item.description} filter={article=>article.tags.some(tag=>slugify(tag)===slug)}/>}
