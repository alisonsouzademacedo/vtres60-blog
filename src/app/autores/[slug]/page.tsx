import type { Metadata } from "next";import { notFound } from "next/navigation";import { TaxonomyPage } from "@/components/editorial/taxonomy-page";import { editorialRepository } from "@/services/editorial";
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params;const item=await editorialRepository.getAuthor(slug);return item?{title:`${item.name} — Autor`,description:item.bio,alternates:{canonical:`/autores/${slug}`}}:{}}
export default async function Page({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const item=await editorialRepository.getAuthor(slug);if(!item)notFound();return <TaxonomyPage eyebrow={item.role} title={item.name} description={item.bio} filter={article=>article.author.slug===slug}/>}
