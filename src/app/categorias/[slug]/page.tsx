import type { Metadata } from "next";import { notFound } from "next/navigation";import { TaxonomyPage } from "@/components/editorial/taxonomy-page";import { editorialRepository } from "@/services/editorial";
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params;const item=await editorialRepository.getCategory(slug);return item?{title:item.metaTitle,description:item.metaDescription,keywords:item.keywords,alternates:{canonical:`/categorias/${slug}`}}:{}}
export default async function Page({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const item=await editorialRepository.getCategory(slug);if(!item)notFound();return <TaxonomyPage eyebrow="Categoria" title={item.name} description={item.seoIntroduction||item.description} filter={article=>article.categorySlug===slug}/>}
