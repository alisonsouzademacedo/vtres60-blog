import { NextResponse } from "next/server";import { requireAdmin } from "@/lib/admin-api";import { apiError,required } from "@/lib/editorial-api";import { editorialRepository } from "@/services/editorial";import type { EducationalArticle } from "@/types/editorial";
export async function GET(){const denied=await requireAdmin();return denied??NextResponse.json(await editorialRepository.listEducationalArticles())}
export async function POST(request:Request){const denied=await requireAdmin();if(denied)return denied;try{const body=await request.json() as Omit<EducationalArticle,"id"|"createdAt"|"updatedAt">;if(!required(body,["title","slug","excerpt","content"]))return apiError(new Error("Título, slug, resumo e conteúdo são obrigatórios."));const item=await editorialRepository.createEducationalArticle(body);const{operationsRepository}=await import("@/services/operations");await operationsRepository.log("criação","artigos",`Artigo “${item.title}” criado.`);return NextResponse.json(item,{status:201})}catch(error){return apiError(error)}}
