import { NextResponse } from "next/server";import { requireAdmin } from "@/lib/admin-api";import { apiError,required } from "@/lib/editorial-api";import { editorialRepository } from "@/services/editorial";import type { ManagedAuthor } from "@/types/editorial";
export async function GET(){const denied=await requireAdmin();return denied??NextResponse.json(await editorialRepository.listAuthors())}
export async function POST(request:Request){const denied=await requireAdmin();if(denied)return denied;try{const body=await request.json() as Omit<ManagedAuthor,"id"|"createdAt"|"updatedAt">;if(!required(body,["name","slug","role"]))return apiError(new Error("Nome, slug e cargo são obrigatórios."));return NextResponse.json(await editorialRepository.createAuthor(body),{status:201})}catch(error){return apiError(error)}}
